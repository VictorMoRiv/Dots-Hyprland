# ============================================================================
# Hyprlock Configuration with Music Player Widget
# ============================================================================

# ----------------------------------------------------------------------------
# Background
# ----------------------------------------------------------------------------
background {
    monitor =
    path = $HOME/Imágenes/Wallpapers/Clearday.jpg
    blur_passes = 0
    contrast = 0.8916
    brightness = 0.8172
    vibrancy = 0.1696
    vibrancy_darkness = 0.0
}

shape {
    monitor = 
    size = 30%, 100%
    color = rgba(239, 241, 245, 0.95)
    rounding = 0
    border_size = 1
    border_color = rgb(230, 233, 239)
    rotate = 0
    xray = false
    position = 0, 0
    halign = left
    valign = center
    zindex = 0
}
# ----------------------------------------------------------------------------
# General Settings
# ----------------------------------------------------------------------------
general {
    ignore_empty_input = true
    hide_cursor = false
}

# ----------------------------------------------------------------------------
# Animations
# ----------------------------------------------------------------------------
animations {
    animation = fade,    1, 6, default
    animation = fadeIn,  1, 6, default
    animation = fadeOut, 1, 6, default
}

# ----------------------------------------------------------------------------
# Password Input Field
# ----------------------------------------------------------------------------
input-field {
    monitor =
    size = 250, 55
    outline_thickness = 2
    dots_size = 0.2      # Scale of input-field height (0.2 - 0.8)
    dots_spacing = 0.2   # Scale of dots' absolute size (0.0 - 1.0)
    dots_center = true
    outer_color = rgb(140, 143, 161)
    inner_color = rgb(239, 241, 245)
    font_color = rgb(76, 79, 105)
    fade_on_empty = false
    font_family = Quicksand Bold
    placeholder_text = <span foreground="##4c4f69">Ingresar Contraseña</span>
    hide_input = false
    position = 150, -170
    halign = left
    valign = center
    zindex = 1
}

# ----------------------------------------------------------------------------
# Clock & Date Display
# ----------------------------------------------------------------------------

# Time (large display)
label {
    monitor =
    text = $TIME
    color = rgb(76, 79, 105)
    font_size = 140
    font_family = Quicksand
    position = 60, 260
    halign = left
    valign = center
    zindex = 1
}

# Date (day and month)
label {
    monitor =
    text = cmd[update:1000] echo -e "$(date +"%A, %d %B")"
    color = rgb(92, 95, 119)
    font_size = 25
    font_family = Quicksand
    position = 130, 380
    halign = left
    valign = center
    zindex = 1
}

# Lock Icon
label {
    monitor =
    text = <span>󰊗 </span>
    color = rgb(221, 120, 120)
    font_size = 28
    font_family = SF Pro Display Bold
    position = 265, -50
    halign = left
    valign = top
    zindex = 1
}

# ----------------------------------------------------------------------------
# User Profile
# ----------------------------------------------------------------------------

# Profile photo
image {
    monitor =
    path = $HOME/Imágenes/tamaulipas.jpg
    border_color = rgb(92, 95, 119)
    border_size = 4
    size = 150
    rounding = 10      # -1 = circle
    rotate = 0
    reload_time = -1
    reload_cmd = 
    position = 200, 30
    halign = left
    valign = center
    zindex = 1
}

# Username greeting
label {
    monitor =
    text = Buenos Días $USER
    color = rgb(76, 79, 105)
    font_size = 25
    font_family = Quicksand
    position = 130, -105
    halign = left
    valign = center
    zindex = 1
}

# ============================================================================
# Music Player Widget
# ============================================================================

# ----------------------------------------------------------------------------
# Album Art
# ----------------------------------------------------------------------------
image {
    monitor =
    path = $HOME/.config/hypr/scripts/music.webp
    size = 100
    rounding = 5
    border_size = 5
    border_color = rgb(204, 208, 218)
    rotate = 0
    reload_time = 1
    reload_cmd = ~/.config/hypr/scripts/hyprlock-music.sh --art
    position = 60, 117
    halign = left
    valign = bottom
    zindex = 2
}

# ----------------------------------------------------------------------------
# Player Background Box
# ----------------------------------------------------------------------------
shape {
    monitor =
    color = rgb(239, 241, 245)
    size = 500, 140
    rounding = 10
    position = 40, 100
    halign = left
    valign = bottom
    zindex = 1
}

# ----------------------------------------------------------------------------
# Track Information
# ----------------------------------------------------------------------------

# Song title
label {
    monitor =
    text = cmd[update:1000] echo "$(~/.config/hypr/scripts/hyprlock-music.sh --title)"
    color = rgb(76, 79, 105)
    font_size = 16
    font_family = Quicksand Italic
    position = 185, 200
    halign = left
    valign = bottom
    zindex = 2
}

# Player source (e.g., Spotify, Firefox)
label {
    monitor =
    text = cmd[update:1000] echo "$(~/.config/hypr/scripts/hyprlock-music.sh --player)"   
    color = rgb(64, 160, 43)
    font_size = 18
    font_family = Quicksand Bold
    position = 495, 110
    halign = left
    valign = bottom
    zindex = 2
}

# Artist name
label {
    monitor =
    text = cmd[update:1000] echo "󰠃  $(~/.config/hypr/scripts/hyprlock-music.sh --artist)"
    color = rgb(223, 142, 29)
    font_size = 12
    font_family = Quicksand Bold
    position = 185, 178
    halign = left
    valign = bottom
    zindex = 2
}

# ----------------------------------------------------------------------------
# Playback Controls
# ----------------------------------------------------------------------------

# Previous track button
label {
    monitor =
    text = 󰒮
    color = rgb(92, 95, 119)
    font_size = 25
    onclick = playerctl previous
    position = 285, 115
    halign = left
    valign = bottom
    zindex = 2
}

# Play/Pause button
label {
    monitor =
    text = cmd[update:1000] ~/.config/hypr/scripts/hyprlock-music.sh --status
    color = rgb(76, 79, 105)
    font_size = 25
    onclick = playerctl play-pause
    position = 325, 115
    halign = left
    valign = bottom
    zindex = 2
}

# Next track button
label {
    monitor =
    text = 󰒭
    color = rgb(92, 95, 119)
    font_size = 25
    onclick = playerctl next
    position = 365, 115
    halign = left
    valign = bottom
    zindex = 2
}

# ----------------------------------------------------------------------------
# Progress Display
# ----------------------------------------------------------------------------

# Current position timestamp
label {
    monitor = 
    text = cmd[update:1000] echo "$(~/.config/hypr/scripts/hyprlock-music.sh --position)"
    color = rgb(92, 95, 119)
    font_size = 10
    font_family = SF Pro Display Medium
    position = 200, 152
    halign = left
    valign = bottom
    zindex = 2
}

# Total length timestamp
label {
    monitor = 
    text = cmd[update:1000] echo "$(~/.config/hypr/scripts/hyprlock-music.sh --length)"
    color = rgb(92, 95, 119)
    font_size = 10
    font_family = SF Pro Display Medium
    position = 475, 152
    halign = left
    valign = bottom
    zindex = 2
}

# Progress bar (with Pango markup for colored segments)
label {
    monitor = 
    text = cmd[update:1000] ~/.config/hypr/scripts/hyprlock-music.sh --progress-bar
    color = rgb(92, 95, 119)
    font_size = 15
    font_family = SF Pro Display Medium
    position = 245, 150
    halign = left
    valign = bottom
    zindex = 2
}


# ============================================================================
# Alternative Progress Bar Styles (Commented Out)
# ============================================================================
# These provide different visual styles for the progress bar.
# Uncomment to use instead of the Pango markup version above.

# Style 1: Backgroundl bar (full width, static gray)
#label {
#    monitor = 
#    text = ━━━━━━━━━━━━━━━━━
#    color = rgba(216, 222, 233, 0.30)
#    font_size = 12
#    font_family = SF Pro Display Medium
#    position = 880, 95
#    halign = left
#    valign = bottom
#}

# Style 2: Foreground bar (dynamic width based on progress, overlays on top)
#label {
#    monitor = 
#    text = cmd[update:1000] bash -c 'p=$(~/.config/hypr/scripts/hyprlock-music.sh --progress); chars=$((p*17/100)); printf "━%.0s" $(seq 1 $chars)'
#    color = rgba(216, 222, 233, 0.70)
#    font_size = 12
#    font_family = SF Pro Display Medium
#    position = 880, 95
#    halign = left
#    valign = bottom
#}
